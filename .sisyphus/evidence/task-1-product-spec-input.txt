TASK 1: Add product-spec.json optional input
==============================================

VERIFICATION: product-spec.json integration

1. product-spec.json mentions: 5 (at least 3 required)
   Command: grep -c 'product-spec.json' resources/com/blockether/spel/templates/agents/spel-visual-qa.md
   Result: 5 ✓

2. OPTIONAL marker present:
   Command: grep 'product-spec.json' resources/com/blockether/spel/templates/agents/spel-visual-qa.md | grep 'OPTIONAL'
   Result: - `product-spec.json` (OPTIONAL, from `spel-product-analyst`) — when present, auto-populate page list from `navigation_map.pages[]` (URL, title, type). Gracefully degrade to manual page specification if absent. ✓

3. navigation_map schema referenced:
   Command: grep 'navigation_map' resources/com/blockether/spel/templates/agents/spel-visual-qa.md
   Result: 
   - `product-spec.json` (OPTIONAL, from `spel-product-analyst`) — when present, auto-populate page list from `navigation_map.pages[]` (URL, title, type). Gracefully degrade to manual page specification if absent.
   - 1. Read `navigation_map.pages[]` from the product spec ✓

ADDED:
- Contract Inputs: product-spec.json (OPTIONAL) with graceful degradation
- New section: "Auto-discovery from product-spec.json"
  - Explains how to read navigation_map.pages[] from product spec
  - Filters to status: "ok" pages only
  - Gracefully degrades if absent or malformed
  - Never requires product-spec.json (always optional)
  - Enables seamless integration with spel-product-analyst output
