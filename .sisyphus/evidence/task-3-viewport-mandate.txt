TASK 3: Add viewport mandate to product-analyst Phase 6 coherence audit
========================================================================

VERIFICATION RESULTS:
=====================

1. Phase count (must be exactly 7):
   Result: 7 phases ✓

2. Viewport mention in Phase 6 (grep -A10 'Phase 6' | grep -i 'viewport'):
   Result: Found ✓
   Output:
   **Viewport testing for `responsive_behavior`**: Before scoring this dimension, capture accessibility snapshots and screenshots at 3 viewports using `spel set-viewport-size`:
   See AGENT_COMMON.md § Mandatory viewport audit for the shared methodology. Score `responsive_behavior` based on observed differences across all 3 viewports, not a single-viewport assumption.

3. responsive_behavior dimension reference (grep 'responsive_behavior'):
   Result: Found 2 matches ✓
   Output:
   **Viewport testing for `responsive_behavior`**: Before scoring this dimension, capture accessibility snapshots and screenshots at 3 viewports using `spel set-viewport-size`:
   See AGENT_COMMON.md § Mandatory viewport audit for the shared methodology. Score `responsive_behavior` based on observed differences across all 3 viewports, not a single-viewport assumption.

4. AGENT_COMMON reference with viewport (grep 'AGENT_COMMON' | grep -i 'viewport'):
   Result: Found ✓
   Output:
   See AGENT_COMMON.md § Mandatory viewport audit for the shared methodology. Score `responsive_behavior` based on observed differences across all 3 viewports, not a single-viewport assumption.

5. Viewport dimensions present (grep 'set-viewport-size\|1280.*720\|768.*1024\|390.*844'):
   Result: Found all 3 dimensions ✓
   Output:
   **Viewport testing for `responsive_behavior`**: Before scoring this dimension, capture accessibility snapshots and screenshots at 3 viewports using `spel set-viewport-size`:
   - Desktop: 1280×720
   - Tablet: 768×1024
   - Mobile: 390×844

6. Lint check:
   Result: PASS ✓
   (Pre-existing info messages only, no errors)

CHANGES MADE:
=============
- File: resources/com/blockether/spel/templates/agents/spel-product-analyst.md
- Location: After line 241 (Audit output requirements section)
- Content: Added viewport mandate paragraph with 3 viewport sizes and AGENT_COMMON reference
- Phase structure: Unchanged (still 7 phases)
- Scoring scale: Unchanged (0-100)
- Interpretation thresholds: Unchanged

COMMIT MESSAGE:
===============
feat(product-analyst): add viewport mandate to Phase 6 coherence audit
